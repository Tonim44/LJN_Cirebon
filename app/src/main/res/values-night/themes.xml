<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Ljn_cirebon" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#F79216</item>
        <item name="colorPrimaryDark">@color/orange</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="IconCard">
        <item name="contentPadding">8dp</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardElevation">7dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="Rectanggle1">
        <item name="contentPadding">8dp</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="cardBackgroundColor">#F79216</item>
        <item name="cardElevation">7dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="Rectanggle2">
        <item name="contentPadding">8dp</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="cardBackgroundColor">#00FFFFFF</item>
        <item name="cardElevation">7dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>
</resources>